/**
 * 侵权/免责声明类弹窗壳（原 TortWarningPopup.js），逻辑多在预制体按钮上。
 */

import { _decorator } from 'cc';
import { PopupBase } from './PopupBase';

const { ccclass } = _decorator;

@ccclass('TortWarningPopup')
export default class TortWarningPopup extends PopupBase {}
